import Loader from 'react-loader-spinner';

const Preloader = () => (
  <Loader
    className="loader"
    type="ThreeDots"
    color="#f2ff7b"
    height={100}
    width={100}
  />
);
export default Preloader;
